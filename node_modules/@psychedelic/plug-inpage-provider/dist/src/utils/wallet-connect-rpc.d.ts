import WalletConnect from "@walletconnect/client";
import { HttpAgent } from "@dfinity/agent";
import { SerializedPublicKey, SimplifiedRPC, WalletConnectOptions } from "../Provider/interfaces";
declare class WalletConnectRPC implements SimplifiedRPC {
    wcClient: WalletConnect;
    wcBridgeURL: string;
    window: any;
    focusUri: any;
    agent: HttpAgent | null;
    isAndroid: boolean;
    isApple: boolean;
    debug: boolean;
    whitelist: string[];
    publicKey: SerializedPublicKey | null;
    constructor(walletConnectOptions: WalletConnectOptions);
    start(): void;
    call(handler: any, args: any, options?: {
        timeout: number;
    }): Promise<unknown>;
    resetSession(): Promise<void>;
    private _call;
    private requestConnect;
    private requestCall;
    private requestQuery;
    private requestReadState;
    private verifyWhitelist;
    private disconnect;
    private clearClient;
    private isAllWhiteListed;
    private addToWhiteList;
    private batchTransactions;
}
export default WalletConnectRPC;
