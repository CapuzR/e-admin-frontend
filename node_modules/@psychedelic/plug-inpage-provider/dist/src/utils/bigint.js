"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.recursiveParseBigint = void 0;
const recursiveParseBigint = (value) => {
    if (value instanceof Object) {
        return Array.isArray(value)
            ? value.map((val) => (0, exports.recursiveParseBigint)(val))
            : Object.entries(value).reduce((acum, [key, val]) => (Object.assign(Object.assign({}, acum), { [key]: (0, exports.recursiveParseBigint)(val) })), {});
    }
    if (typeof value === "bigint") {
        return parseInt(value.toString(), 10);
    }
    return value;
};
exports.recursiveParseBigint = recursiveParseBigint;
