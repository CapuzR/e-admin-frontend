import { ActorSubclass, HttpAgent } from '@dfinity/agent';
import { Principal } from '@dfinity/principal';
import NFT_Origyn from '../../interfaces/nft_origyn';
import NFT from './default';
import { NFTDetails, NFTCollection } from '../../interfaces/nft';
export default class NFTOrigyn extends NFT<string, string> {
    standard: string;
    actor: ActorSubclass<NFT_Origyn>;
    constructor(canisterId: string, agent: HttpAgent);
    getUserTokens(principal: Principal): Promise<NFTDetails<string>[]>;
    getMetadata(): Promise<NFTCollection>;
    transfer(to: Principal, tokenIndex: string): Promise<void>;
    details(tokenIndex: string): Promise<NFTDetails<string>>;
    private serializeTokenData;
}
