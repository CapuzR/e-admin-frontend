import { DetailType, DetailValue, Metadata } from "../interfaces/dab_registries/registry_standard";
export declare type FormattedMetadata = Omit<Metadata, 'details'> & {
    details: Details;
};
export interface Details {
    [key: string]: DetailType;
}
export declare const parseDetailValue: (detailValue: DetailValue) => DetailType;
export declare const formatRegistryDetails: (details: Metadata['details']) => Details;
export declare const formatMetadata: (metadata: Metadata) => FormattedMetadata;
