import type { ActorSubclass } from "@dfinity/agent";
export declare const useCanister: <T>(canisterName: string, options?: {
    mode: string;
}) => readonly [ActorSubclass, {
    canisterDefinition: any;
    error: any;
    loading: boolean;
}];
